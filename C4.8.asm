; Àý4.8


STACK       SEGMENT STACK 'STACK'
            DW 100H DUP(?)
TOP         LABEL WORD
STACK       ENDS
			N1=20
			N2=30
DATA        SEGMENT
BUFA		DB 10H,25H,67H,26H,68H,73H,83H,58H,0,06H,12H,0CDH,95H
			DB 0A8H,0C2H,48H,0E6H,0F1H,1AH,0F5H
BUFB		DB 05, 12H, 26H, 45H, 53H,60H,6AH, 7FH, 76H, 88H, 92H, 0C1H, 0DEH 
			DB 0E1H,0F5H, 09, 17H, 23H, 48H, 58H, 65H, 67H, 70H, 7CH, 82H, 96H 
			DB 0CDH, 0D1H, 0F1H, 0EEH 
BUFC		DB 20 DUP(0)
BUFCA		DB 20 DUP(0)
BUFCB		DB 20 DUP(0)
DATA		ENDS
CODE		SEGMENT
			ASSUME CS:CODE,DS:DATA,ES:DATA,SS:STACK

START:
		MOV AX,DATA
		MOV DS,AX
		MOV ES,AX
		MOV AX,STACK
		MOV SS,AX
		LEA SP,TOP
		
		MOV CX,N1
		XOR SI,SI
		XOR DI,DI
		
L1:		
		MOV AL,BUFA[SI]
		PUSH CX
		MOV CX,N2
		XOR BX,BX
		
L2:
		CMP AL,BUFB[BX]
		JZ L3
		INC BX
		LOOP L2
		JMP L4
L3:
		MOV BUFC[DI],AL
		MOV DX,SI
		MOV BUFCA[DI],DL
		MOV BUFCB[DI],BL
		INC DI
		
L4:		
        POP CX
		INC SI
		LOOP L1
		MOV AH,4CH
		MOV AL,0
		INT 21H
		
CODE 	ENDS
		END START



















