; Àý4.22


STACK       SEGMENT STACK 'STACK'
            DW 100H DUP(?)
TOP         LABEL WORD
STACK       ENDS
DATA        SEGMENT
X           DD 3234H
Y           DD 5678H
STRING1		DB 'PLEASE INPUT DATA: $'
DATA		ENDS
CODE		SEGMENT
			ASSUME CS:CODE,DS:DATA,ES:DATA,SS:STACK

START:
		MOV AX,DATA
		MOV DS,AX
		MOV ES,AX
		MOV AX,STACK
		MOV SS,AX
		LEA SP,TOP
		CALL FUNC3
		INT 21H
		
FUNC3	PROC NEAR
		MOV DX,X+2
		MOV AX,X
		ADD AX,AX
		ADC DX,DX
		JC C10
		CMP DX,Y+2
		JB C20
		JA C10
		CMP AX,Y
		JBE C20
C10:	MOV AX,1
		JMP C30
C20:	MOV AX,-1
C30:	RET
FUNC3 	ENDP
	
CODE 	ENDS
		END START



















