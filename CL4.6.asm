; œ∞Ã‚4.6


STACK 	SEGMENT STACK 'STACK'
		DW 100H DUP(?)
TOP 	LABEL WORD
STACK 	ENDS
DATA 	SEGMENT
    VAR1 DW 6D55H
    VAR2 DW 4A55H
    VAR3 DW 6D55H
DATA 	ENDS
CODE 	SEGMENT 
		ASSUME CS:CODE,DS:DATA,ES:DATA,SS:STACK  

START:
    MOV AX,DATA
    MOV DS,AX
    MOV ES,AX
    MOV AX,STACK
    MOV SS,AX
    LEA SP,TOP
    XOR AX,AX

    MOV AX,-1
    MOV BX,VAR1
    CMP BX,VAR2
    JE L2
    CMP BX,VAR3
    JE L1
    MOV AX,BX
    LEA SI,VAR1
    JMP OVER
L1:
    MOV AX,VAR2
    LEA SI,VAR2
    JMP OVER
L2:
    CMP BX,VAR3
    JZ OVER
    MOV AX,VAR3
    LEA SI,VAR3
OVER:
         
CODE ENDS 
     END START
