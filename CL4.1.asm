; œ∞Ã‚4.1


		N=10
STACK 	SEGMENT STACK 'STACK'
		DW 100H DUP(?)
TOP 	LABEL WORD
STACK 	ENDS
DATA 	SEGMENT
SOURCE 	LABEL BYTE
BUF		DB 'ABCDE12345'
BUFR	DB N DUP(0)  
        DB N DUP(0)
DATA 	ENDS
CODE 	SEGMENT 
		ASSUME CS:CODE,DS:DATA,ES:DATA,SS:STACK  

 START:
    MOV AX,DATA
    MOV DS,AX
    MOV ES,AX
    MOV AX,STACK
    MOV SS,AX
    LEA SP,TOP
    XOR AX,AX

    MOV CX,N
    LEA DI,BUF
	LEA SI,BUFR
	MOV BX,N 
	DEC BX
LOOP1:  
    MOV DX,[DI][BX]
    MOV [SI],DL  
    DEC BX
    INC SI
    LOOP LOOP1    

CODE ENDS 
     END START    
